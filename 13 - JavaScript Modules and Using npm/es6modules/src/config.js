export const apiKey = 'abc123'; // named export - have to use apiKey

export const url = 'https://markfasel.com';

export function sayHi(name) {
	console.log(`Hello there ${name}`);
}

//export default apiKey;